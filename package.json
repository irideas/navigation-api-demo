{
  "name": "navigation-api-demo",
  "version": "1.0.0",
  "description": "Navigation API vs History+Location API 对比测试项目",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-404.js",
    "build:prod": "vite build --mode production && VITE_BASE_PATH=/navigation-api-demo/ node scripts/generate-404.js",
    "preview": "vite preview",
    "serve": "vite preview --port 3000",
    "deploy": "npm run build:prod && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
